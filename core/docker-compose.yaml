services:
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  app-1:
    build: .
    environment:
      SPRING_PROFILES_ACTIVE: docker
      RATE_LIMIT_ADMIN_TOKEN: rl-admin-7f3a9c2e8b1d4f6a9e0c3b5d7f2a8e1
    depends_on:
      redis:
        condition: service_healthy
    expose:
      - "8080"

  app-2:
    build: .
    environment:
      SPRING_PROFILES_ACTIVE: docker
      RATE_LIMIT_ADMIN_TOKEN: rl-admin-7f3a9c2e8b1d4f6a9e0c3b5d7f2a8e1
    depends_on:
      redis:
        condition: service_healthy
    expose:
      - "8080"

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app-1
      - app-2